name: Cron Fetch

on:
  schedule:
    - cron: "*/10 * * * *"   # 每 10 分鐘跑一次
  workflow_dispatch:         # 手動觸發（方便測試）

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Cron Endpoint
        run: |
          curl -s -X GET "https://mercari-line-bot.vercel.app/cron"
